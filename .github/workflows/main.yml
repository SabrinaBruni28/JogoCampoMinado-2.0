name: Build Campo Minado

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    if: "!contains(github.event.head_commit.message, 'Update Readme')"
    name: Build Windows Executable
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
  
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
  
      - name: Create virtual environment
        shell: cmd
        run: python -m venv .venv
  
      - name: Install dependencies
        shell: cmd
        run: .venv\Scripts\pip.exe install -r requirements.txt
  
      - name: Build with PyInstaller
        shell: cmd
        run: |
          call .venv\Scripts\activate.bat
          pyinstaller --name CampoMinado --onefile --windowed --icon=Images/campo-minado.ico --add-data "Images;Images" --add-data "Sounds;Sounds" Jogo/interface.py
  
      - name: Check dist folder contents
        shell: cmd
        run: dir dist
  
      - name: Upload Windows Executable
        uses: actions/upload-artifact@v4
        with:
          name: CampoMinado-Windows
          path: dist/CampoMinado.exe

  build-linux:
    if: "!contains(github.event.head_commit.message, 'Update Readme')"
    name: Build Linux Executable
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt

      - name: Build with PyInstaller
        run: |
          source .venv/bin/activate
          pyinstaller --name CampoMinado --onefile --windowed \
            --icon=Images/campo-minado.ico \
            --add-data "Images:Images" \
            --add-data "Sounds:Sounds" \
            Jogo/interface.py

      - name: Upload Linux Executable
        uses: actions/upload-artifact@v4
        with:
          name: CampoMinado-Linux
          path: dist/CampoMinado

  build-macos:
    if: "!contains(github.event.head_commit.message, 'Update Readme')"
    name: Build macOS Executable
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt
          pip install pillow

      - name: Convert .ico to .icns
        run: |
          source .venv/bin/activate
          python -c "from PIL import Image; Image.open('Images/campo-minado.ico').save('Images/campo-minado.icns')"

      - name: Build with PyInstaller
        run: |
          source .venv/bin/activate
          pyinstaller --name CampoMinado --onefile --windowed \
            --icon=Images/campo-minado.icns \
            --add-data "Images:Images" \
            --add-data "Sounds:Sounds" \
            Jogo/interface.py

      - name: Upload macOS Executable
        uses: actions/upload-artifact@v4
        with:
          name: CampoMinado-macOS
          path: dist/CampoMinado

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-windows, build-linux, build-macos]

    permissions:
      contents: write

    steps:
      - name: Download Windows Artifact
        uses: actions/download-artifact@v4
        with:
          name: CampoMinado-Windows
          path: release/windows

      - name: Download Linux Artifact
        uses: actions/download-artifact@v4
        with:
          name: CampoMinado-Linux
          path: release/linux

      - name: Download macOS Artifact
        uses: actions/download-artifact@v4
        with:
          name: CampoMinado-macOS
          path: release/macos

      - name: Rename files
        run: |
          mv release/windows/CampoMinado.exe release/CampoMinado-Windows.exe
          mv release/linux/CampoMinado release/CampoMinado-Linux
          mv release/macos/CampoMinado release/CampoMinado-macOS

      - name: Fix Linux executable permission
        run: chmod +x release/CampoMinado-Linux

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: Campo Minado v${{ github.run_number }}
          draft: false
          prerelease: false
          files: |
            release/CampoMinado-Windows.exe
            release/CampoMinado-Linux
            release/CampoMinado-macOS

